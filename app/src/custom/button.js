import React, { useState, useEffect } from 'react';

import '../fonts.css';
import './button.css';

const Button = () => {
	return (
		<div className='btn'>
         <button className="button ">Создать список</button>
        </div>
    );
};

export default Button;
